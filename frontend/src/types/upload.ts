export type UploadData = {
  public_id: string;
};

export type signatureData = {
  signature: string;
  timestamp: string;
  cloudName: string;
  apiKey: string;
  folder: string;
  publicId: string;
};